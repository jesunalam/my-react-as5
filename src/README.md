Project's name:my-react-ass5

Des:I build the site with my best tried to recreate exect the same page. I wanna also mention that i use a little bit ai to boosted my project.Atleast i enjoy while build the site 

Tec:Toastify,react-icon
Feature:sticky,click-event,delete after select stack




<!-- jsx -->
JSX is a syntax extension for React that lets you write HTML directly inside JavaScript to easily describe UI components.
<!-- props -->
props are used to pass component while state hold the internal data  render or re render as need
<!-- state hook -->
the state hook are used to manages dynamic internal data within a React component,like as inputs, toggles, or fetched API data that require UI re-renders when updated.
<!-- key -->
Key props give elements a unique identity so React can easily  track, update, and re-order items in a list without re-rendering the whole DOM
<!-- conditional rendaring -->
conditional rendaring mean show the thing with the condition state  and i use in btn ${true?add to stack: added}
<!-- props pass -->
Props are passed from a parent component to a child component as custom attributes which is known as props drilling